import sys

# read input line
line = input();

# split the input to numbers
input_numbers = line.split();

# convert to individual int variables
X = int(input_numbers[0]);
Y = int(input_numbers[1]);
N = int(input_numbers[2]);

# loop through all values in the range - FILL IN THE IF CONDITIONS
for i in range(1, N+1):
    if i%X == 0 and i%Y == 0:
        print("FizzBuzz");
    elif   i%X==0   :
        print("Fizz");
    elif   i%Y==0   :
        print ("Buzz");
    else:
        print (i);
